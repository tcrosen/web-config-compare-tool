# Web.config Compare Tool

A simple Node utility that runs comparison unit tests against two Web.config files.

This was created so I can automatically test Web.config transformations before deploying.

